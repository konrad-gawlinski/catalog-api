database:
  username: user
  password: pass
  host: localhost
  database: default
country:
  available: ['de']
language:
  available: ['de_de']
product:
  config:
    validation_rules: config.yml
  bundle:
    validation_rules: config.yml
